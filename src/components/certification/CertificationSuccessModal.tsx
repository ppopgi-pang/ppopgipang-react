import Modal, { type ModalProps } from '../common/modal/modal';

export default function CertificationSuccessModal(props: ModalProps) {
    return (
        <Modal {...props} title="방문인증">
            방문인증
        </Modal>
    );
}
